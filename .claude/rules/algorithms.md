---
description: Rules for pure algorithm functions
paths:
  - 'src/algorithms/**/*.ts'
---

# Algorithm Rules

- All functions must be pure (no side effects)
- Every constant must have a research citation in constants.ts
- TDD is MANDATORY: write failing test first, then implement
- Per-session cap: 8-12 direct sets per muscle (no junk volume)
- Pull volume >= push volume in every split (shoulder health)
- Use component-based TDEE (BMR + NEAT + EAT + TEF), not single multiplier
